# SPEC_INSTRUCTIONS
Instruction-level executable contract docs.

Each instruction file must define:
- Inputs (args/accounts)
- Preconditions (requires)
- Effects (state/token/event)
- Failure modes (error codes)
- Postconditions (invariants)
